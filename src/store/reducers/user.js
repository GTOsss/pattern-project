import {

} from '../constants/index';

const initialState = {
  isLogin: false,
};

export default function user(state = initialState, { type, payload }) {
  switch (type) {
    default:
      return state;
  }
}
